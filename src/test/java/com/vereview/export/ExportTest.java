package com.vereview.export;

import com.vereview.TestUtils;
import com.vereview.message.FileMessage;
import org.apache.commons.lang3.exception.ExceptionUtils;
import org.junit.Assert;
import org.junit.Test;

public class ExportTest {

    /*
    @Test
    public void copyNativeTest(){
        try {
            Export e = new Export();
            FileMessage fm = TestUtils.getTestNativeCopyMessage();
            e.copyNative(fm);
        }catch (Throwable t){
            Assert.fail(ExceptionUtils.getStackTrace(t));
        }
    }

     */
}
