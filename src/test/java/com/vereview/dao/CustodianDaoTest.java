package com.vereview.dao;

import org.apache.commons.lang3.exception.ExceptionUtils;
import org.junit.Assert;
import org.junit.Test;

import java.util.Map;

/**
 * Created by mjmangan on 8/13/17.
 */
public class CustodianDaoTest extends DaoTest{

    /*
    @Test
    public void findAllTest(){
        try {
            CustodianDao dao = new CustodianDao(mgr);
            Map<Long, String> cs = dao.findAll();
            cs.forEach((k,v) -> {
                System.out.println("custodianId: " + k + " | name: " + v);
            });
            System.out.println(cs.size());
        }catch (Throwable t){
            Assert.fail(ExceptionUtils.getStackTrace(t));
        }
    }
    */
}
